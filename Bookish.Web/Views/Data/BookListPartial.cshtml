@model BookListModel;

@foreach (BookForWeb book in Model.BooksList) {
    <tr>
        <th scope="row">@book.Isbn</th>
        <td>@book.Title</td>
        <td>@book.Author</td>
        <td>@book.NumCopiesOwned</td>
        <td>@book.NumCopiesAvailable</td>
    </tr>
}